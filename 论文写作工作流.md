---
source_url: https://st1020.com/posts/write-thesis-with-markdown-part1/
author:
  - T!gger.
created: 2026-09-07
project:
type: Note
tags:
  - 论文
  - 工作流
  - zotero
  - pandoc
status: stable
---
# 论文写作工作流

> 使用 `Markdown` 作为写作语言

Markdown 是一个非常简单的标记语言，它非常简洁，易于阅读，易于编写。

Markdown 完全没有包含样式，完全做到了**格式与内容分离**，编写时可以完全沉浸在内容中，而不会被其他东西干扰。

更重要的是，因为它足够简单，所以可以很方便地作为中间语言被转换为其他格式，借助 `Pandoc`，它可以轻松转换为 `docx`

只需对图、表做少量的排版工作即可完成所有格式调整。

## 工作流

### 编辑器

使用 VSCode 配合插件，直接使用 md 语法写作，完全不需要考虑格式和排版

### 文献管理和文献和图片的交叉引用

为了自动进行文献引用，需要两个文件：

- `ref.bib` - 文献引用列表
- `china-national-standard-gb-t-7714-2015-numeric.csl` - 文献引用样式

其中 `ref.bib` 由 `Zotero` 自动生成，对所有浏览过的论文都可以统一管理，无需人为干预。

`china-national-standard-gb-t-7714-2015-numeric.csl` 为国标引文样式规范。

对所有引文，在正文中用 `[@citekey]` 标注，由 `Pandoc` 自动转换为规范样式。

如果需要 Cite While You Write 的话，可以使用 VSCode 插件 [Pandoc Citer](https://marketplace.visualstudio.com/items?itemName=notZaki.pandocciter) 或者 [Citation Picker for Zotero](https://marketplace.visualstudio.com/items?itemName=mblode.zotero)。

除此之外，如果需要对图表、公式或者表格的交叉引用的话，可以使用 [pandoc-crossref](https://github.com/lierdakil/pandoc-crossref)。

### 格式转换

最终需要提交的论文产物需要为 docx/pdf 格式，这时需要用到 [Pandoc](https://pandoc.org/)。

另外，如果学校提供 docx 格式的论文模板（必须为模板，而非在正文调整好格式的文档），则可以通过一条命令直接将论文转换为对应格式，且无需进行任何格式调整。