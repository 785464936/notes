---
project: "[[考研]]"
type: MOC
tags:
  - 学习
  - 考研
  - 英语
  - 目录
author:
  - T!gger.
created: 2026-06-28
status: stable
---
# 英语


```dataviewjs
dv.list(
  dv.pages()
    .where(p => p.tags && p.tags.includes("英语"))
    .file.link
)
```

```dataviewjs
const m = /^考研英语真题试题-(\d{4})$/;

const rows = dv.pages()
  .where(p => m.test(p.file.name))
  .array()
  .map(p => {
    const y = p.file.name.match(m)[1];
    const ans = dv.pages(`"${p.file.folder}"`).where(a => a.file.name === `考研英语真题答案-${y}`);
    return { y, link: p.file.link, ans: ans.length ? ans[0].file.link : "—" };
  })
  .sort((a, b) => Number(b.y) - Number(a.y))
  .map(r => [r.link, r.ans]);

dv.table([], rows);
```