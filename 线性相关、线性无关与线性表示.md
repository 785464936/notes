---
author: T!gger.
project: 考研学习
created: 2026-05-26
tags:
  - 学习
  - 考研
  - 线代
..: "[[线代]]"
---
# 线性相关、线性无关与线性表示

## 定义

### 线性相关

- 定义： $\exists$ 一组不全为 $0$ 的 $k$，使 $k\alpha_{1}+k\alpha_{2}+\dots+k_{s}\alpha_{s}=0$ (**存在不全为 0 的**)
- 齐次方程组 $(\alpha_{1}，\alpha_{2},\dots,\alpha_{s})\vec{k}=0$ 有**非零**解
- $r(\alpha_{1}，\alpha_{2},\dots,\alpha_{s})<s$

### 线性无关

- 定义： 仅在 $k＝0$ 时 $k\alpha_{1}+k\alpha_{2}+\dots+k_{s}\alpha_{s}=0$（**必须全为 0**）
- 齐次方程组 $(\alpha_{1}，\alpha_{2},\dots,\alpha_{s})\vec{k}=0$ **只有 $0$ 解**
- $r(\alpha_{1}，\alpha_{2},\dots,\alpha_{s})＝s$

### 线性表示

- 定义： $\beta＝k\alpha_{1}+k\alpha_{2}+\dots+k_{s}\alpha_{s}$
- 非齐次方程组 $(\alpha_{1}，\alpha_{2},\dots,\alpha_{s})\vec{k}=0$ **有解**
- $r(\alpha_{1}，\alpha_{2},\dots,\alpha_{s})＝r(\alpha_{1}，\alpha_{2},\dots,\alpha_{s},\beta)$
    - 即 **系数矩阵的秩等于增广的秩**

### 不能表出

- 非齐次方程组 $(\alpha_{1}，\alpha_{2},\dots,\alpha_{s})\vec{k}=0$ **无解**
- $r(\alpha_{1}，\alpha_{2},\dots,\alpha_{s})<r(\alpha_{1}，\alpha_{2},\dots,\alpha_{s},\beta)$
    - 即 **系数矩阵的秩小于增广的秩**

### 推广

向量组 $B: \beta_1, \beta_2, \cdots, \beta_t$ 可由向量组 $A: \alpha_1, \alpha_2, \cdots, \alpha_s$ 线性表示

$$\begin{align}
& \Leftrightarrow \text{方程 } AX=B \text{ 有解} \\
& \Leftrightarrow r(\alpha_1, \alpha_2, \cdots, \alpha_s) = r(\alpha_1, \alpha_2, \cdots, \alpha_s, \beta_1, \beta_2, \cdots, \beta_t)
\end{align}
$$

如果向量组 $\beta_1, \beta_2, \cdots, \beta_t$ 可由向量组 $\alpha_1, \alpha_2, \cdots, \alpha_s$ 线性表示, 则

$$
r(\beta_1, \beta_2, \cdots, \beta_t) \leqslant r(\alpha_1, \alpha_2, \cdots, \alpha_s)
$$

## 判断相关性的常用方法

### 定义

设 $k_1 \alpha_1 + k_2 \alpha_2 + \cdots + k_s \alpha_s = \mathbf{0}$, 若 $k_1 = k_2 = \cdots = k_s = 0$, 则 $\alpha_1, \alpha_2, \cdots, \alpha_s$ 线性无关

![[定义法判断相关性.jpg]]

### 方程组

齐次方程组有非 0 解 $\implies$ 相关

### 秩

列满秩 $\implies$ 相关

### 假设反证

### 行列式

$\lvert A \rvert=0$ 相关

> $A^{m-1}\alpha\neq0, A^{m}\alpha=0$，则 $\alpha,A\alpha,\dots,A^{m-1}\alpha$ 线性无关 #做题方法 

## 相关结论

### 部分相关则整体相关，整体无关则部分无关 （向量个数） #做题方法 

### 原相关则短相关，原无关则长无关（向量维数） #做题方法 

### 以少表多，多必相关 #做题方法 

设向量组 $\alpha_1, \alpha_2, \cdots, \alpha_s$ 可由 $\beta_1, \beta_2, \cdots, \beta_t$ 线性表出且 $t < s$, 那么 $\alpha_1, \alpha_2, \cdots, \alpha_s$ 线性相关

“以少表多，多必相关”逆否定理
如果向量组 $\alpha_1, \alpha_2, \cdots, \alpha_s$ 可由向量组 $\beta_1, \beta_2, \cdots, \beta_t$ 线性表出, 且 $\alpha_1, \alpha_2, \cdots, \alpha_s$ 线性无关, 那么 $t \geqslant s$

> 助记: 线性无关的向量组中没有混子向量, 如果你想比线性无关的向量组还要 NB, 那就必须拿出更多的向量才行

### 定义

- 一个向量 $\alpha$ (个数 $s=1$) 线性相关 $\Leftrightarrow \alpha = 0$
- 两个向量相关 $\Leftrightarrow$ 它们的分量对应成比例

### 行列式

- $n$ 维向量组 $\alpha_1, \alpha_2, \cdots, \alpha_n$ 线性相关 $\Leftrightarrow |\alpha_1, \alpha_2, \cdots, \alpha_n| = 0$
- $n$ 维向量组 $\alpha_1, \alpha_2, \cdots, \alpha_n$ 线性无关 $\Leftrightarrow |\alpha_1, \alpha_2, \cdots, \alpha_n| \neq 0$

### 方程组

- 初等行变换不改变列向量组的线性相关性原理解释: 设 $p_1, p_2, \cdots, p_n$ 为 $1, 2, \cdots, n$ 的一个排列, $\alpha_1, \alpha_2, \cdots, \alpha_s$ 和 $\beta_1, \beta_2, \cdots, \beta_s$ 为两向量组, 其中
$$
\alpha_i = \begin{bmatrix} a_{1i} \\ a_{2i} \\ \vdots \\ a_{ni} \end{bmatrix}, \quad \beta_i = \begin{bmatrix} a_{p_1 i} \\ a_{p_2 i} \\ \vdots \\ a_{p_n i} \end{bmatrix}
$$
即 $\beta_1, \beta_2, \cdots, \beta_s$ 是对 $\alpha_1, \alpha_2, \cdots, \alpha_s$ 各分量顺序进行重排后得到的向量组, 则这两个向量组有相同的线性相关性

### 秩

- 包含零向量的向量组是线性相关的
- 向量个数 $s$ 大于维数 $n$ 一定相关
- 设向量组 $\alpha_1, \alpha_2, \cdots, \alpha_s$ 线性无关, 而向量组 $\alpha_1, \alpha_2, \cdots, \alpha_s, \beta$ 线性相关, 则 $\beta$ 能由向量组 $\alpha_1, \alpha_2, \cdots, \alpha_s$ 线性表出, 且表示式是唯一的
    - > 助记: 向量组的秩可以理解为向量组表出能力, 秩越小, 向量组的表出能力就越弱, 谁能表示别人, 谁就 NB, 秩就更大
- 向量组 $\beta_1, \beta_2, \cdots, \beta_t$ 能由向量组 $\alpha_1, \alpha_2, \cdots, \alpha_s$ 线性表示的充分必要条件是 $r(\alpha_1, \alpha_2, \cdots, \alpha_s) = r(\alpha_1, \alpha_2, \cdots, \alpha_s, \beta_1, \beta_2, \cdots, \beta_t)$

> 零向量性质: 
> - 包含 0 的向量组相关
> - 0 与任意向量相关
> - 0 与任意向量正交