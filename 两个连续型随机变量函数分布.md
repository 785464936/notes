---
author:
  - T!gger.
created: 2026-08-07
project: "[[考研]]"
type: Note
tags:
  - 学习
  - 考研
  - 概率
..: "[[概率]]"
status: stable
---

# 两个连续型随机变量函数分布

$(X,Y) \sim f(x,y)$, $Z=g(X,Y)$ 求 $Z$ 的分布

## 分布函数法

$$
    F_Z(z) = P\{Z \le z\} = P\{g(X,Y) \le z\} = \iint_{g(x,y) \le z} f(x,y) dxdy
$$

(讨论 $z$ 的范围)

$$
f_Z(z) = F'_Z(z)
$$

## 卷积公式

### 两个连续型随机变量和的卷积公式

$Z = X+Y$

- $\displaystyle f_Z(z) = \int_{-\infty}^{+\infty} f(x, z-x) dx$
- $\displaystyle f_Z(z) = \int_{-\infty}^{+\infty} f(z-y, y) dy$

### 两个连续型随机变量一般线性组合的卷积公式

$Z = aX+bY, \quad a \ne 0, b \ne 0$

- $\displaystyle f_Z(z) = \frac{1}{|b|} \int_{-\infty}^{+\infty} f(x, \frac{z-ax}{b}) dx$
- $\displaystyle f_Z(z) = \frac{1}{|a|} \int_{-\infty}^{+\infty} f(\frac{z-by}{a}, y) dy$

### 两个连续型随机变量积(商)的卷积公式

$Z=XY$ 或 $Z=\dfrac{Y}{X}$

1) $Z=XY$ 时
    - $\displaystyle f_Z(z) = \int_{-\infty}^{+\infty} f(x, \frac{z}{x}) \frac{1}{|x|} dx$
    - $\displaystyle f_Z(z) = \int_{-\infty}^{+\infty} f(\frac{z}{y}, y) \frac{1}{|y|} dy$

2) $Z=\dfrac{Y}{X}$ 时
    - $f_Z(z) = \int_{-\infty}^{+\infty} f(x, zx) |x| dx$

> - $X \sim N(0,1), Y \sim N(0,1)$ 且 $X, Y$ 独立
>     - 若 $Z=X+Y$, 则 $Z \sim N(0,2)$

> 推广
> - $X \sim N(\mu_1, \sigma_1^2), Y \sim N(\mu_2, \sigma_2^2), X, Y$ 独立
> - $Z = k_1 X \pm k_2 Y$
> - $Z \sim N(k_1 \mu_1 + k_2 \mu_2, k_1^2 \sigma_1^2 + k_2^2 \sigma_2^2)$ ($k_1, k_2$ 不全为 0)

## 最大、最小分布

$X \sim F_X(x), Y \sim F_Y(y)$ 且 $X, Y$ 独立，$M = \max\{X, Y\}, N = \min\{X, Y\}$

$$
\begin{aligned}
F_M(x) &= P\{M \le x\} = P\{\max\{X, Y\} \le x\} \\
&= P\{X \le x\} P\{Y \le x\} \quad \text{(自变量取值任意)} \\
&= F_X(x) F_Y(x) \quad \text{(X,Y 同分布)} = F(x)^2 \\

F_N(x) &= P\{N \le x\} = P\{\min\{X, Y\} \le x\} \\
&= 1 - P\{\min\{X, Y\} > x\} \\
&= 1 - P\{X > x\} P\{Y > x\} \\
&= 1 - [1 - P\{X \le x\}] [1 - P\{Y \le x\}] \\
&= 1 - (1 - F_X(x))(1 - F_Y(x)) \quad \text{(X,Y 同分布)} = 1-[1-F(x)]^{2}
\end{aligned}
$$

> 推广到 $n$ 个 (此处以同分布举例)
> - $F_M(x) = F(x)^n$
> - $F_N(x) = 1 - [1 - F(x)]^n$