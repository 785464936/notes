---
type: Template
tags:
  - mermaid
status: stable
---
```mermaid

```<%*
setTimeout(() => {
    app.fileManager.processFrontMatter(tp.config.target_file, (frontmatter) => {
        delete frontmatter["type"];
    });
}, 100);
%>