---
author:
  - T!gger.
created: 2026-08-18
project: "[[考研]]"
type: Note
tags:
  - 学习
  - 考研
..: "[[线代]]"
status: stable
---
# 矩阵相似对角化

## 矩阵相似对角化的定义

设 $A\alpha_i = \lambda_i \alpha_i \ (i=1, 2, \cdots, n)$，则

$$
A(\alpha_1, \alpha_2, \cdots, \alpha_n) = (\lambda_1\alpha_1, \lambda_2\alpha_2, \cdots, \lambda_n\alpha_n) = (\alpha_1, \alpha_2, \cdots, \alpha_n) \Lambda
$$

当 $\alpha_1, \alpha_2, \cdots, \alpha_n$ 线性无关时，记 $P = (\alpha_1, \alpha_2, \cdots, \alpha_n)$，则 $P$ 可逆，记 $\Lambda = \text{diag}(\lambda_1, \lambda_2, \cdots, \lambda_n)$

> $\alpha_1, \alpha_2, \cdots, \alpha_n$ 必须线性无关

故 $AP = P\Lambda$，即 $P^{-1}AP = \Lambda$，称 $A$ 相似于 $\Lambda$，记作 $A \sim \Lambda$

## 矩阵相似对角化的常用结论

### 充要条件

设 $A$ 是 $n$ 阶矩阵，则 $A \sim \Lambda$

$$
\begin{align}
& \Leftrightarrow A \text{ 恰有 } n \text{ 个线性无关的特征向量} \\
& \Leftrightarrow \text{矩阵 } A \text{ 每个特征值的几何重数等于代数重数}
\end{align}
$$

### 充分条件

设 $A$ 是 $n$ 阶矩阵，则

- $A$ 有 $n$ 个不同的特征值 $\Rightarrow A \sim \Lambda$
- $A$ 为实对称矩阵 $\Rightarrow A \sim \Lambda$
- $r(A) = 1$，且 $tr(A) \neq 0 \Rightarrow A \sim \Lambda$
> 以上可以直接用
- $(A - aE)(A - bE) = O$，且 $a \neq b \Rightarrow A \sim \Lambda$
- 特别地，$A^2 = A \Rightarrow A \sim \Lambda$，$A^2 = E \Rightarrow A \sim \Lambda$
> 先证明才能用

### 必要条件

若 $n$ 阶矩阵 $A$ 满足 $A \sim \Lambda$，则

- $|A| = |\Lambda|$
- $tr(A) = tr(\Lambda)$
- $r(A) = r(\Lambda)$

### 常用性质

若 $n$ 阶矩阵 $A$ 满足 $A \sim \Lambda$，则

- $f(A) \sim f(\Lambda)$
- $A^{-1} \sim \Lambda^{-1}$
- $A^* \sim \Lambda^*$

> 若可逆 $A \sim \Lambda \Leftrightarrow A^* \sim \Lambda^*, A^{-1} \sim \Lambda^{-1}$

> - 可以利用上述充要条件、充分条件，以及利用相似的传递性来说明 $A \sim \Lambda$
>
> - 可以利用不满足必要条件说明 $A \nsim \Lambda$

## 矩阵相似对角化的一般求解步骤

1. 求出 $A$ 的所有的特征值 $\lambda_1, \lambda_2, \cdots, \lambda_n$，其中互不相等的特征值为 $\lambda_{p1}, \lambda_{p2}, \cdots, \lambda_{pr} \ (r \le n)$
2. 若 $A$ 可对角化，则 $k$ 重特征值 $\lambda$ 必对应 $k$ 个线性无关的特征向量，求出每一个齐次方程组 $(\lambda_{pi}E - A)x = 0 \ (i=1, 2, \cdots, r)$ 的基础解系，合并后必可得到 $A$ 的 $n$ 个线性无关的特征向量 $x_1, x_2, \cdots, x_n$
3. 令 $P = (x_1, x_2, \cdots, x_n)$，$\Lambda = \operatorname{diag}(\lambda_1,\lambda_2,\dots,\lambda_n)$，则 $P$ 可逆，且有 $P^{-1}AP = \Lambda$，一定要注意这里 $P$ 的每一列 $x_j \ (j=1, 2, \cdots, n)$ 的排序应与 $\Lambda$ 中对应的 $\lambda_j \ (j=1, 2, \cdots, n)$ 的排序相同