---
author:
  - T!gger.
created: 2026-08-02
project: "[[考研]]"
type: Note
tags:
  - 学习
  - 考研
  - 概率
..: "[[概率]]"
status: stable
---

# 最大似然估计

## 离散型

$X \sim P\{X=x_i\} = p_i(\theta) \quad (i=1,2,\cdots)$

取$X_1, X_2, \cdots, X_n$ 的观测值 $x_1, x_2, \cdots, x_n$

$$
\begin{aligned}
&P\{X_1=x_1, X_2=x_2, \cdots, X_n=x_n; \theta\} \\
&= P\{X_1=x_1; \theta\} \cdots P\{X_n=x_n; \theta\} \quad \text{(独立)} \\
&= \prod_{i=1}^n P\{X_i=x_i; \theta\} = L(\theta)
\end{aligned}
$$

> Tip
#做题方法 
1. 写似然函数 $L(\theta)$
2. 取对数 $\ln L(\theta)$ (变为连加法)
3. 令 
$$\dfrac{d \ln L(\theta)}{d\theta} = 0 \implies \begin{cases}
驻点唯一 & \hat{\theta} = \\
驻点不唯一 & 根据题目取舍 \\
无驻点 & 
\begin{cases}
    > 0 & L(\theta) \uparrow 取最小值点 \\
    < 0 & L(\theta) \downarrow 取最大值点 \end{cases}
\end{cases}
$$

## 连续型

$X \sim f(x; \theta)$ 取 $X_1, \cdots, X_n$ 的观测值 $x_1, \cdots, x_n$

$L(\theta) = f(x_1, \cdots, x_n; \theta) = f(x_1; \theta) \cdots f(x_n; \theta)$

$L(\theta) = \displaystyle \prod_{i=1}^n f(x_i; \theta)$

1. 写似然函数 $L(\theta)$
2. 取对数 $\ln L(\theta)$ (变为连加法)
3. 令 
$$\dfrac{d \ln L(\theta)}{d\theta} = 0 \implies \begin{cases}
驻点唯一 & \hat{\theta} = \\
驻点不唯一 & 根据题目取舍 \\
无驻点 & 
\begin{cases}
    > 0 & L(\theta) \uparrow 取最小值点 \\
    < 0 & L(\theta) \downarrow 取最大值点 \end{cases}
\end{cases}
$$