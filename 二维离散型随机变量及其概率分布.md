---
author:
  - T!gger.
created: 2026-08-30
project: "[[考研]]"
type: Note
tags:
  - 学习
  - 考研
  - 概率
..: "[[概率]]"
status: stable
---
# 二维离散型随机变量及其概率分布

## 联合分布律

$$
    P\{ X=x_{i}, Y=y_{i} \} = p_{ij} \quad i,j = 1,2,\dots
$$

> - $p_{ij}\geq 0$
> - $\displaystyle \sum_{i}\sum_{j}p_{ij}=1$

> 定取值  
> 算概率  
> 验证 1

## 边缘分布律

若联合分布律已知

$$
    \begin{align}
    P\{ X=x_{i} \} = P\{ X=x_{i},Y<+\infty \}=\sum_{j=1}^{+\infty} P\{ X=x_{i},Y=y_{j} \}=\sum_{j=1}^{+\infty} p_{ij} \\
    P\{ Y=y_{i} \} = P\{ X<+\infty,Y=y_{i} \}=\sum_{i=1}^{+\infty} P\{ X=x_{i},Y=y_{j} \}=\sum_{i=1}^{+\infty} p_{ij}
    \end{align}
$$

## 条件分布律

$P\{X=x_i\} > 0$ 时

$$
P\{Y=y_j \mid X=x_i\}
=\frac{P\{X=x_i,\,Y=y_j\}}{P\{X=x_i\}},\quad j=1,2,\cdots
$$

$P\{Y=y_j\} > 0$ 时

$$
P\{X=x_i \mid Y=y_j\}
=\frac{P\{X=x_i,\,Y=y_j\}}{P\{Y=y_j\}},\quad i=1,2,\cdots
$$

> 即 $\displaystyle \dfrac{\text{联合}}{\text{边缘}}$

## 两个离散型随机变量的独立性

若 $P\{X=x_i, Y=y_j\} = P\{X=x_i\}P\{Y=y_j\}$

对 $\forall i,j$ 成立，则 $X$ 与 $Y$ 独立

### 求联合分布律

- 若 $X, Y$ 独立，联合 = 边缘 $\times$ 边缘
- 若 $X, Y$ 不独立，联合 = 边缘 $\times$ 条件
- 一般情况
    - 定取值
    - 算概率
    - 验证 1

### 判定独立性

- 若已知联合，先求边缘
- 验证 联合 $?=$ 边缘 $\times$ 边缘
- 若联合未知，解题方向为判定不独立

### 同分布

- **分布律** 或 **密度函数** 或 **分布函数** 相同

## 两个离散型随机变量的函数的分布

$$
    P\{X=x_i, Y=y_j\} = p_{ij} \quad i,j=1,2,\dots
$$

$Z=g(X,Y)$ 一定为离散型，它的分布律

$$
    P\{Z=z_k\} = P\{g(X,Y)=z_k\} = \sum_{g(x_i,y_j)=z_k} P\{X=x_i, Y=y_j\}
$$
