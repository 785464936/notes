---
cssclasses:
  - table_without_headers
author:
  - T!gger.
created: 2026-08-18
project: "[[考研]]"
type: Note
tags:
  - 学习
  - 考研
..: "[[线代]]"
status: stable
---
# 特征值与特征向量

## 特征值与特征向量

$\exists \lambda; {x}_{n}\neq {0}$, 使得

$${Ax}=\lambda {x}$$

称 $\lambda$ 为 ${A}$ 的**[^特征值]**，${x}$ 是 ${A}$ 的对应与特征值 $\lambda$ 的**[^特征向量]**

> 本章仅针对方阵，非方阵无特征值

> ${x}\neq {0}$，但 $\lambda$ 可为 0

## 特征方程与特征多项式

对 ${Ax}=\lambda {x}$ 移项得 $$(\lambda {E}-{A}){x}={0}$$

解 $|\lambda {E}-{A}|=0$ 得 $\lambda$

称 $|\lambda {E}-{A}|=0$ 为**[^特征方程]**，$|\lambda {E}-{A}|$ 为**[^特征多项式]**

## 求解特征值与特征向量的一般方法

1. 解特征方程 $|\lambda {E}-{A}|=0$ ，得到 $k$ 个根为矩阵 ${A}$ 的 $n$ 个特征值
    - 根的重数 $k$ 称为**[^代数重数]**，$\sum k=n$
2. 将特征值代入特征矩阵，求出 $(\lambda {E}-{A}){x}={0}$ 的基础解系，得到特征向量
    - 对于一个 $\lambda$，其对应的基础解系有 $p$ 个线性无关的向量，称 $p$ 为**[^几何重数]**，$p\geq 1$
    - $p=n-r(\lambda_{i}{E}-{A})$

> - 任一 $n$ 阶矩阵 ${A}$ 必有 $n$ 个（复）特征值  
> - $1\leq p\leq k$ ，即 $k$ 重特征值最多有 $k$ 个线性无关的特征向量
> - $|{A}|=0 \Leftrightarrow {A}$ 的列向量线性相关 $\Leftrightarrow {Ax}={0}$ 有非零解 $\Leftrightarrow{A}$ 有 $0$ 特征值 #xxx 
> - 抽象问题从定义入手 ${Ax}=\lambda {x}$

## 特征向量的性质

- 同一特征值的特征向量任意线性组合仍是该特征值的特征向量
- 不同特征值的特征向量必无关
- ${A}$ 的两个互异特征值对应的特征向量非零线性组合得到的一定不是 ${A}$ 的特征向量

| | | | | | | | | |
| :--: | :--: | :--: | :--: | :--: | :--: | :--: | :--: | :--: |
|矩阵|${A}$|$k{A}+bE$|${A}^{k}$|$f({A})$|${A}^{-1}$|${A}^{*}$|${A}^{\mathrm{T}}$|${P}^{-1}{A}{P}$|
|特征值|$\lambda$|$l\lambda +b$|$\lambda^{k}$|$f(\lambda)$|$\dfrac{1}{\lambda}$|$\dfrac{\lvert {A} \rvert}{\lambda}$|$\lambda$|$\lambda$|
|特征向量|${x}$|${x}$|${x}$|${x}$|${x}$|${x}$|另算|${P}^{-1}{x}$|

> - 表格中 $A^{-1}$，$A^*$ 所在列，需注意 $\lambda \neq 0$  
> - 若矩阵 $A$ 满足 $f(A) = O$，则矩阵 $A$ 对应的特征值必满足 $f(\lambda) = 0$，但 $f(\lambda) = 0$ 的解不一定都是矩阵 $A$ 对应的特征值  
> - 设 $n$ 阶方阵 $A$ 的特征多项式为 $|\lambda E - A| = \lambda^n + a_{n-1}\lambda^{n-1} + \cdots + a_1\lambda + a_0$，则 $A^n + a_{n-1}A^{n-1} + \cdots + a_1A + a_0E = O$
