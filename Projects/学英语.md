```dataviewjs
dv.list(
  dv.pages()
    .where(p => p.project === "学英语" && 
    !p.file.path.includes("Templates") && 
    !(p.tags && p.tags.includes("english-word")) && 
    !(p.tags && p.tags.includes("phrases"))
    )
    .file.link
)
```