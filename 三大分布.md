---
author:
  - T!gger.
project: "[[考研]]"
created: 2026-07-29
tags:
  - 学习
  - 考研
  - desmos-graph
..: "[[  ]]"
---
# 三大分布

$\chi^2, t, F$

### 密度函数的大致图像

 (均可查表)

## $\chi^2$ 分布

- $X_i \sim N(0, 1) \quad i=1, \cdots n$
- $X_i$ 相互独立
- 则 $X_1^2 + \cdots X_n^2 = \displaystyle\sum_{i=1}^n X_i^2 \sim \chi^2(n)$ (自由度)

### 大致图像

```desmos-graph
bottom = -1; top = 7;
left = -1; right = 15;
width = 360; height = 180;
---
k=1
f(x) = 7 * x * e^{-x/2} \left\{x>0\right\}
0<y<f(x)
```

### 上 $\alpha$ 分位点

- $P\{X^2 > \chi^2_\alpha(n)\} = \alpha$

### 性质

- 若 $X \sim \chi^2(m), Y \sim \chi^2(n)$ 且 $X, Y$ 独立, 则 $X+Y \sim \chi^2(m+n)$
- $E\chi^2(n) = n, \quad D\chi^2(n) = 2n$

> Tip: 正态分布平方的方差转为卡方的方差计算

## $t$ 分布

- $X \sim N(0, 1) \quad Y \sim \chi^2(n) \quad X, Y$ 独立
- 则 $\dfrac{X}{\sqrt{Y/n}} \sim t(n)$ (自由度)

### 大致图像

```desmos-graph
bottom = -0.5; top = 2.5;
left = -4; right = 4;
width = 360; height = 180;
---
f(x)=5e^{-0.5*x*x}/\sqrt{2*\pi}
```

### 上 $\alpha$ 分位点

- $P\{t > t_\alpha(n)\} = \alpha$ 

### 性质

- $t_{1-\alpha}(n) = -t_\alpha(n)$

## $F$ 分布

- $X \sim \chi^2(m), Y \sim \chi^2(n) \quad X, Y$ 独立, 则
- $\dfrac{X/m}{Y/n} \sim F(m, n)$ (第一自由度, 第二自由度)

### 大致图像

```desmos-graph
bottom = -0.5; top = 2;
left = -0.5; right = 4;
width = 360; height = 180;
---
d_1=1
d_2=1
f(x) =20 \frac{15}{4}\cdot \left(\frac{2}{3}\right)^2 x \left(1+\frac{2}{3}x\right)^{-5} \left\{x>0\right\}
```

### 上 $\alpha$ 分位点

- $P\{F > F_\alpha(m, n)\} = \alpha$

### 性质

- 若 $F \sim F(m, n)$ 则 $\dfrac{1}{F} \sim F(n, m)$
- $\dfrac{1}{F_\alpha(m, n)} = F_{1-\alpha}(n, m)$

> 证: $P\{F < F_\alpha(m, n)\} = \alpha$
> $\Rightarrow P\{\dfrac{1}{F} > \dfrac{1}{F_\alpha(m, n)}\} = \alpha$
> $\Rightarrow P\{\dfrac{1}{F} > \dfrac{1}{F_\alpha(m, n)}\} = 1-\alpha$
> $\Rightarrow \dfrac{1}{F_\alpha(m, n)} = F_{1-\alpha}(n, m)$

## $t$ 与 $F$ 的关系

- $X \sim t(n)$, 则 $X^2 \sim F(1, n)$