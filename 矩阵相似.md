---
author:
  - T!gger.
created: 2026-08-18
project: "[[考研]]"
type: Note
tags:
  - 学习
  - 考研
..: "[[线代]]"
status: stable
---
# 矩阵相似

## 矩阵相似的定义

设 $A$ 与 $B$ 都是 $n$ 阶矩阵，若存在一个可逆 $n$ 阶矩阵 $P$，使得 $P^{-1}AP = B$，则称 $A$ 相似于 $B$，或称 $A$ 经过了一个相似变换得到了 $B$，记为 $A \sim B$

> 抽象型问题多用 $AP = PB$

> 等价 $A \cong B$
> 相似 $A \sim B$
> 合同 $A \simeq B$

## 矩阵相似的常用必要条件

$$
A \sim B \Rightarrow
\begin{cases}
|\lambda E - A| = |\lambda E - B|
    \begin{cases}
        |A| = |B| \\
        tr(A) = tr(B) \\
        \lambda_A = \lambda_B
    \end{cases} \\
r(A) = r(B) \\
r(\lambda E - A) = r(\lambda E - B) \\
\end{cases}
$$

## 矩阵相似的性质

- 传递性：若 $A \sim C, B \sim C$，则 $A \sim B$
- 若 $A \sim B$，则 $\boxed{kA \sim kB}, A^m \sim B^m, f(A) \sim f(B)$
    - 其中 $f(A) = a_n A^n + \cdots + a_1 A^1 + a_0 E, f(B) = a_n B^n + \cdots + a_1 B^1 + a_0 E$
- 若 $A \sim B$，还可推得 $\boxed{A^T \sim B^T}, A^* \sim B^*$
- 若 $A \sim B$，且 $A$ 可逆，则 $\boxed{A^{-1} \sim B^{-1}}, f(A^{-1}) \sim f(B^{-1}), AB \sim BA$
- 若 $A \sim C, B \sim D$，则 
$$\begin{bmatrix}
A&O \\
O&B
\end{bmatrix}\sim
\begin{bmatrix}
C&O \\
O&D
\end{bmatrix}$$

> $\boxed{* * *}$ 为充要条件，其他为充分条件

## 判断/证明两个矩阵相似

- 考研范畴内，判断/证明两个矩阵相似主要靠定义（通过题目的提示验证 $P^{-1}AP = B$）或相似的传递性（$A \sim C, B \sim C$，则 $A \sim B$）来处理，其中，“中介矩阵”$C$ 常常用对角矩阵 $\Lambda$
- 流程图语言理解矩阵相似的传递过程（重点）
    - 问题：$A \sim B$，且 $A$ 与 $B$ 矩阵均可相似对角化，求矩阵 $P$ 使得 $P^{-1}AP = B$
    - 流程图过程：
        $A \sim B$，有 $A \sim \Lambda \sim B$
        $P_1^{-1} A P_1 = \Lambda$
        $P_2^{-1} B P_2 = \Lambda$
        $\Rightarrow P_2 P_1^{-1} A P_1 P_2^{-1} = B$
        令 $P = P_1 P_2^{-1}$，则 $P^{-1} A P = B$
- 思考：在题目未给出提示，且 $A$ 与 $B$ 矩阵均不可相似对角化时，如何判断/证明 $A$ 与 $B$ 矩阵相似？
    - 定义，$AP = PB$ （假设 $P$），转化为方程组问题